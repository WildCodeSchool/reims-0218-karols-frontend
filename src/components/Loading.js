import React from "react"
import ReactLoading from "react-loading"

const Loading = ({ type, color }) => (
  <ReactLoading type={"spin"} color={"#000000"} height={400} width={300} />
)

export default Loading
