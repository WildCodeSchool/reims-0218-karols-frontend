import React from "react"
import { Button, Input, FormGroup } from "reactstrap"

const ValidationReservationButton = () => {
  return (
    <FormGroup>
      <Button color="primary" size="lg" active>
        Récapitulatif
      </Button>
    </FormGroup>
  )
}

export default ValidationReservationButton
